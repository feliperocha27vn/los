/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetPagesQueryResponse,
  GetPagesQueryParams,
} from "../types/GetPages.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getPages } from "../clients/getPages.ts";

export const getPagesSuspenseQueryKey = (params?: GetPagesQueryParams) =>
  [{ url: "/pages" }, ...(params ? [params] : [])] as const;

export type GetPagesSuspenseQueryKey = ReturnType<
  typeof getPagesSuspenseQueryKey
>;

export function getPagesSuspenseQueryOptions(
  params?: GetPagesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPagesSuspenseQueryKey(params);
  return queryOptions<
    GetPagesQueryResponse,
    ResponseErrorConfig<Error>,
    GetPagesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getPages(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List study pages
 * {@link /pages}
 */
export function useGetPagesSuspense<
  TData = GetPagesQueryResponse,
  TQueryKey extends QueryKey = GetPagesSuspenseQueryKey,
>(
  params?: GetPagesQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetPagesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getPagesSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getPagesSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
