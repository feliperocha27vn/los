/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type { GetCoursesQueryResponse } from "../types/GetCourses.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getCourses } from "../clients/getCourses.ts";

export const getCoursesSuspenseQueryKey = () => [{ url: "/courses" }] as const;

export type GetCoursesSuspenseQueryKey = ReturnType<
  typeof getCoursesSuspenseQueryKey
>;

export function getCoursesSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getCoursesSuspenseQueryKey();
  return queryOptions<
    GetCoursesQueryResponse,
    ResponseErrorConfig<Error>,
    GetCoursesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getCourses({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List study courses
 * {@link /courses}
 */
export function useGetCoursesSuspense<
  TData = GetCoursesQueryResponse,
  TQueryKey extends QueryKey = GetCoursesSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetCoursesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getCoursesSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getCoursesSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
