/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetPagesQueryResponse,
  GetPagesQueryParams,
} from "../types/GetPages.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getPages } from "../clients/getPages.ts";

export const getPagesQueryKey = (params?: GetPagesQueryParams) =>
  [{ url: "/pages" }, ...(params ? [params] : [])] as const;

export type GetPagesQueryKey = ReturnType<typeof getPagesQueryKey>;

export function getPagesQueryOptions(
  params?: GetPagesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPagesQueryKey(params);
  return queryOptions<
    GetPagesQueryResponse,
    ResponseErrorConfig<Error>,
    GetPagesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getPages(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List study pages
 * {@link /pages}
 */
export function useGetPages<
  TData = GetPagesQueryResponse,
  TQueryData = GetPagesQueryResponse,
  TQueryKey extends QueryKey = GetPagesQueryKey,
>(
  params?: GetPagesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPagesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getPagesQueryKey(params);

  const query = useQuery(
    {
      ...getPagesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
