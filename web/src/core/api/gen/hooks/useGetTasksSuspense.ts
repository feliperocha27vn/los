/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetTasksQueryResponse,
  GetTasksQueryParams,
} from "../types/GetTasks.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getTasks } from "../clients/getTasks.ts";

export const getTasksSuspenseQueryKey = (params?: GetTasksQueryParams) =>
  [{ url: "/tasks" }, ...(params ? [params] : [])] as const;

export type GetTasksSuspenseQueryKey = ReturnType<
  typeof getTasksSuspenseQueryKey
>;

export function getTasksSuspenseQueryOptions(
  params?: GetTasksQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getTasksSuspenseQueryKey(params);
  return queryOptions<
    GetTasksQueryResponse,
    ResponseErrorConfig<Error>,
    GetTasksQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getTasks(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List tasks
 * {@link /tasks}
 */
export function useGetTasksSuspense<
  TData = GetTasksQueryResponse,
  TQueryKey extends QueryKey = GetTasksSuspenseQueryKey,
>(
  params?: GetTasksQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetTasksQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getTasksSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getTasksSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
