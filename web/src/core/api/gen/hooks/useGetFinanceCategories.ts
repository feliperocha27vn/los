/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetFinanceCategoriesQueryResponse,
  GetFinanceCategoriesQueryParams,
} from "../types/GetFinanceCategories.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getFinanceCategories } from "../clients/getFinanceCategories.ts";

export const getFinanceCategoriesQueryKey = (
  params?: GetFinanceCategoriesQueryParams,
) => [{ url: "/finance/categories" }, ...(params ? [params] : [])] as const;

export type GetFinanceCategoriesQueryKey = ReturnType<
  typeof getFinanceCategoriesQueryKey
>;

export function getFinanceCategoriesQueryOptions(
  params?: GetFinanceCategoriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getFinanceCategoriesQueryKey(params);
  return queryOptions<
    GetFinanceCategoriesQueryResponse,
    ResponseErrorConfig<Error>,
    GetFinanceCategoriesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getFinanceCategories(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List finance categories (seed)
 * {@link /finance/categories}
 */
export function useGetFinanceCategories<
  TData = GetFinanceCategoriesQueryResponse,
  TQueryData = GetFinanceCategoriesQueryResponse,
  TQueryKey extends QueryKey = GetFinanceCategoriesQueryKey,
>(
  params?: GetFinanceCategoriesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetFinanceCategoriesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getFinanceCategoriesQueryKey(params);

  const query = useQuery(
    {
      ...getFinanceCategoriesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
