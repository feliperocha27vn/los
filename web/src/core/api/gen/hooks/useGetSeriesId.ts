/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetSeriesIdQueryResponse,
  GetSeriesIdPathParams,
  GetSeriesId404,
} from "../types/GetSeriesId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getSeriesId } from "../clients/getSeriesId.ts";

export const getSeriesIdQueryKey = (
  id: GetSeriesIdPathParams["id"] | undefined,
) => [{ url: "/series/:id", params: { id: id } }] as const;

export type GetSeriesIdQueryKey = ReturnType<typeof getSeriesIdQueryKey>;

export function getSeriesIdQueryOptions(
  id: GetSeriesIdPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getSeriesIdQueryKey(id);
  return queryOptions<
    GetSeriesIdQueryResponse,
    ResponseErrorConfig<GetSeriesId404>,
    GetSeriesIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getSeriesId(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get series detail
 * {@link /series/:id}
 */
export function useGetSeriesId<
  TData = GetSeriesIdQueryResponse,
  TQueryData = GetSeriesIdQueryResponse,
  TQueryKey extends QueryKey = GetSeriesIdQueryKey,
>(
  id: GetSeriesIdPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetSeriesIdQueryResponse,
        ResponseErrorConfig<GetSeriesId404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getSeriesIdQueryKey(id);

  const query = useQuery(
    {
      ...getSeriesIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetSeriesId404>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
