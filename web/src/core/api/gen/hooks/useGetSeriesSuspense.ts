/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetSeriesQueryResponse,
  GetSeriesQueryParams,
} from "../types/GetSeries.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getSeries } from "../clients/getSeries.ts";

export const getSeriesSuspenseQueryKey = (params?: GetSeriesQueryParams) =>
  [{ url: "/series" }, ...(params ? [params] : [])] as const;

export type GetSeriesSuspenseQueryKey = ReturnType<
  typeof getSeriesSuspenseQueryKey
>;

export function getSeriesSuspenseQueryOptions(
  params?: GetSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getSeriesSuspenseQueryKey(params);
  return queryOptions<
    GetSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    GetSeriesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getSeries(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List series (most recently watched first)
 * {@link /series}
 */
export function useGetSeriesSuspense<
  TData = GetSeriesQueryResponse,
  TQueryKey extends QueryKey = GetSeriesSuspenseQueryKey,
>(
  params?: GetSeriesQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetSeriesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getSeriesSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getSeriesSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
