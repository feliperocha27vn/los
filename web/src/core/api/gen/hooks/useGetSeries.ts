/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetSeriesQueryResponse,
  GetSeriesQueryParams,
} from "../types/GetSeries.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getSeries } from "../clients/getSeries.ts";

export const getSeriesQueryKey = (params?: GetSeriesQueryParams) =>
  [{ url: "/series" }, ...(params ? [params] : [])] as const;

export type GetSeriesQueryKey = ReturnType<typeof getSeriesQueryKey>;

export function getSeriesQueryOptions(
  params?: GetSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getSeriesQueryKey(params);
  return queryOptions<
    GetSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    GetSeriesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getSeries(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List series (most recently watched first)
 * {@link /series}
 */
export function useGetSeries<
  TData = GetSeriesQueryResponse,
  TQueryData = GetSeriesQueryResponse,
  TQueryKey extends QueryKey = GetSeriesQueryKey,
>(
  params?: GetSeriesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetSeriesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getSeriesQueryKey(params);

  const query = useQuery(
    {
      ...getSeriesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
