/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetModulesQueryResponse,
  GetModulesQueryParams,
} from "../types/GetModules.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getModules } from "../clients/getModules.ts";

export const getModulesQueryKey = (params?: GetModulesQueryParams) =>
  [{ url: "/modules" }, ...(params ? [params] : [])] as const;

export type GetModulesQueryKey = ReturnType<typeof getModulesQueryKey>;

export function getModulesQueryOptions(
  params?: GetModulesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getModulesQueryKey(params);
  return queryOptions<
    GetModulesQueryResponse,
    ResponseErrorConfig<Error>,
    GetModulesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getModules(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List study modules
 * {@link /modules}
 */
export function useGetModules<
  TData = GetModulesQueryResponse,
  TQueryData = GetModulesQueryResponse,
  TQueryKey extends QueryKey = GetModulesQueryKey,
>(
  params?: GetModulesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetModulesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getModulesQueryKey(params);

  const query = useQuery(
    {
      ...getModulesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
