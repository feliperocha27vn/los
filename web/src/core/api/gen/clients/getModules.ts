/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  GetModulesQueryResponse,
  GetModulesQueryParams,
} from "../types/GetModules.ts";

function getGetModulesUrl() {
  const res = { method: "GET", url: `/modules` as const };
  return res;
}

/**
 * @summary List study modules
 * {@link /modules}
 */
export async function getModules(
  params?: GetModulesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetModulesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetModulesUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
