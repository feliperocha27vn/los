/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type { GetTrackerTodayQueryResponse } from "../types/GetTrackerToday.ts";

function getGetTrackerTodayUrl() {
  const res = { method: "GET", url: `/tracker/today` as const };
  return res;
}

/**
 * @summary Get today tracker view (habits + records of today)
 * {@link /tracker/today}
 */
export async function getTrackerToday(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTrackerTodayQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetTrackerTodayUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
