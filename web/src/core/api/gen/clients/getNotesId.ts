/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  GetNotesIdQueryResponse,
  GetNotesIdPathParams,
  GetNotesId404,
} from "../types/GetNotesId.ts";

function getGetNotesIdUrl(id: GetNotesIdPathParams["id"]) {
  const res = { method: "GET", url: `/notes/${id}` as const };
  return res;
}

/**
 * @summary Get note detail
 * {@link /notes/:id}
 */
export async function getNotesId(
  id: GetNotesIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetNotesIdQueryResponse,
    ResponseErrorConfig<GetNotesId404>,
    unknown
  >({
    method: "GET",
    url: getGetNotesIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
