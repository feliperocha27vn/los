/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  GetSeriesQueryResponse,
  GetSeriesQueryParams,
} from "../types/GetSeries.ts";

function getGetSeriesUrl() {
  const res = { method: "GET", url: `/series` as const };
  return res;
}

/**
 * @summary List series (most recently watched first)
 * {@link /series}
 */
export async function getSeries(
  params?: GetSeriesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetSeriesQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetSeriesUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
