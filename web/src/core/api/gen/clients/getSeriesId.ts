/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  GetSeriesIdQueryResponse,
  GetSeriesIdPathParams,
  GetSeriesId404,
} from "../types/GetSeriesId.ts";

function getGetSeriesIdUrl(id: GetSeriesIdPathParams["id"]) {
  const res = { method: "GET", url: `/series/${id}` as const };
  return res;
}

/**
 * @summary Get series detail
 * {@link /series/:id}
 */
export async function getSeriesId(
  id: GetSeriesIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetSeriesIdQueryResponse,
    ResponseErrorConfig<GetSeriesId404>,
    unknown
  >({
    method: "GET",
    url: getGetSeriesIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
