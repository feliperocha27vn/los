/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@core/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@core/api/client";
import type {
  GetCoursesIdQueryResponse,
  GetCoursesIdPathParams,
  GetCoursesId404,
} from "../types/GetCoursesId.ts";

function getGetCoursesIdUrl(id: GetCoursesIdPathParams["id"]) {
  const res = { method: "GET", url: `/courses/${id}` as const };
  return res;
}

/**
 * @summary Get study course detail
 * {@link /courses/:id}
 */
export async function getCoursesId(
  id: GetCoursesIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetCoursesIdQueryResponse,
    ResponseErrorConfig<GetCoursesId404>,
    unknown
  >({
    method: "GET",
    url: getGetCoursesIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
