# URL base da API do backend.
# Em desenvolvimento, deixe em branco para que o Vite utilize as regras de proxy locais (definidas em vite.config.ts).
# Em produção (Cloudflare Pages), defina este valor com o endereço do seu backend (ex: https://api.seudominio.com).
VITE_API_URL=
