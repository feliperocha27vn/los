# URL base da API em produção. É build-time (Vite assa no bundle) e pública
# (qualquer um vê no JS do cliente), por isso fica versionada aqui — assim o
# `pnpm build` sempre injeta o valor certo, sem depender de var no dashboard.
VITE_API_URL=https://api.votipet.tech
